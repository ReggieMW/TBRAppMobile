<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TBRAppMobile.Pages.DbBooksPage"
             Title="📚 Saved Books">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="10">
            <Label Text="Books in Database" FontSize="24" FontAttributes="Bold" />

            <CollectionView x:Name="DbBookList"
                            ItemsSource="{Binding Books}"
                            SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="10" Margin="5" CornerRadius="8" HasShadow="True" BackgroundColor="White">
                            <VerticalStackLayout>
                                <Label Text="{Binding Title}" FontAttributes="Bold" FontSize="18" TextColor="Black" />
                                <Label Text="{Binding Author}" FontSize="14" TextColor="Gray" />
                                <Label Text="{Binding Subject}" FontSize="12" TextColor="DimGray" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
